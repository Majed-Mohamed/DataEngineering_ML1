# Jupyter Configuration

This folder contains Jupyter configuration to keep the project structure clean.

## Configuration Applied

- **No checkpoint folders**: Jupyter won't create `.ipynb_checkpoints/` folders
- **Clean workspace**: Your notebooks folder stays organized

## Files

- `jupyter_notebook_config.py` - Disables automatic checkpoint creation

## Note

If you want Jupyter to create checkpoints, delete or rename the config file.
